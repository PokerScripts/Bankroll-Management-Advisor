# Советник по банкролл-менеджменту

**Bankroll-Management-Advisor** — это консольный Python-советник по банкролл-менеджменту и оценке риска разорения для кэш-игр, MTT и SNG. Скрипт помогает определить, достаточно ли банкролла для игры на выбранном лимите, каков риск разорения (Risk of Ruin, RoR), какой банкролл требуется для заданного уровня риска, а также позволяет запускать Монте-Карло симуляции для оценки дисперсии.

---

## 🚀 Возможности

### 🔍 Анализ выбранного лимита
Скрипт рассчитывает:
- математическое ожидание `μ` и стандартное отклонение `σ`,
- риск разорения:
  - при бесконечном горизонте,
  - при конечном горизонте (с учётом количества сессий/турниров),
- минимальный безопасный банкролл для заданного предельного риска.

### 📊 Подбор безопасных лимитов
На основе конфигурационного JSON-файла скрипт:
- оценивает RoR для каждого лимита,
- определяет минимально необходимый банкролл,
- отмечает лимиты как **SAFE** или **RISKY**.

### 🧪 Monte Carlo симуляции
Скрипт позволяет моделировать траектории банкролла:
- множество прогонов (по умолчанию 1000+),
- оценка вероятности разорения,
- распределение финального банкролла,
- анализ максимальных даунсвингов,
- возможность экспорта результатов.

### 🧩 Гибкая конфигурация
Скрипт работает с кэш-играми, турнирами MTT и SNG.  
Все параметры вводятся через CLI.

---

## 📦 Установка

```bash
git clone https://github.com/PokerScripts/Bankroll-Management-Advisor
cd Bankroll-Management-Advisor
python3 bankroll.py --help
````

Python 3.8+ рекомендуется.

Дополнительно скрипт может использовать `numpy` (опционально), но работает и без него.

---

## 🛠 Использование

### 1) Анализ лимита (кэш)

```bash
python bankroll.py analyze-limit \
  --game-type cash \
  --winrate-bb100 5 \
  --stdev-bb100 80 \
  --hands-per-session 1000 \
  --bb-value 0.25 \
  --bankroll 1500 \
  --risk 0.05 \
  --ror-mode infinite
```

### 2) Анализ лимита (MTT)

```bash
python bankroll.py analyze-limit \
  --game-type mtt \
  --roi 0.2 \
  --buyin 11 \
  --stdev-buyins 4 \
  --bankroll 2000 \
  --risk 0.05 \
  --horizon 1000 \
  --ror-mode finite
```

---

## 3) Подбор лимитов

Создайте файл `limits.json`:

```json
{
  "game_type": "cash",
  "bankroll": 1500,
  "risk": 0.05,
  "ror_mode": "infinite",
  "limits": [
    {
      "name": "NL10",
      "bb_value": 0.1,
      "winrate_bb100": 8,
      "stdev_bb100": 70
    },
    {
      "name": "NL25",
      "bb_value": 0.25,
      "winrate_bb100": 5,
      "stdev_bb100": 80
    }
  ]
}
```

Запуск:

```bash
python bankroll.py suggest-limits --config limits.json
```

---

## 4) Монте-Карло симуляция

```bash
python bankroll.py simulate \
  --game-type cash \
  --winrate-bb100 3 \
  --stdev-bb100 90 \
  --hands-per-session 800 \
  --bb-value 0.5 \
  --bankroll 2000 \
  --horizon 500 \
  --trials 2000 \
  --seed 42 \
  --export results.csv
```

---

## 📘 Основные компоненты

* **risk_of_ruin_infinite** — оценка RoR при бесконечном горизонте.
* **risk_of_ruin_finite** — RoR с учётом количества сессий/турниров.
* **required_bankroll_for_risk_infinite** — минимальный BR для заданного RoR.
* **find_required_bankroll_finite** — бинарный поиск минимального BR при конечном горизонте.
* **run_simulations** — Монте-Карло симуляции.
* **analyze-limit / suggest-limits / simulate** — CLI-команды.

---

## 📄 Лицензия

Проект распространяется под лицензией **MIT**. Вы можете свободно использовать, изменять и распространять код.
